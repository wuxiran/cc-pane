import { createRoot } from "react-dom/client";
import ScreenshotSelector from "./components/screenshot/ScreenshotSelector";

createRoot(document.getElementById("root")!).render(<ScreenshotSelector />);
