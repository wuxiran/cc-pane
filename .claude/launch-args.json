{"projectPath": "D:\u0004_workspace_rust\\cc-book", "cliTool": "codex", "runtimeKind": "wsl", "title": "Reviewer: 会话认领实现 + 部署风险", "prompt": "你是独立同行评审者。只读评审，不要执行、不要写代码或文件、不要跑构建或测试。\n\n## 评审对象\n\n主树提交 3167e9e（29 文件 / +1870 −89）。请用只读命令读取：\n\n    cd /mnt/d/04_workspace_rust/cc-book\n    git show --stat 3167e9e\n    git show 3167e9e\n\n设计文档（含事故根因与三阶段方案）：\n\n    /mnt/d/04_workspace_rust/cc-book/docs/61-orphan-session-adoption.md\n\n## 背景一句话\n\nCC-Panes 桌面端反复开关后，新实例认领不到 daemon 里仍活着的 PTY 会话，把每个标签页的会话重建一遍，导致 50 个 claude 进程里 22 个是重复的。本提交是三阶段修复：数据层归属记录、daemon 写权限租约、启动认领轮。\n\n## 最高优先级：部署风险\n\n这是本次评审的首要问题，请务必先答。\n\n修改后的 daemon 二进制**尚未部署**。运行中的 daemon（PID 65304）名下有 61 个直接子进程（23 个 claude、7 个 wsl、30 个 OpenConsole），其中包含用户正在使用的多个会话。要让阶段 2/3 生效必须重启 daemon，而重启会杀掉全部这些进程。\n\n请评估：\n1. 有没有不重启 daemon 就能灰度验证的路径？（例如另起一个 daemon 实例、用 dev 隔离环境、只验证不依赖 daemon 的阶段 1/3 部分）\n2. 如果必须重启，最小损失的顺序是什么？哪些状态需要先落盘？现有的 session_restore 表在重启后能否把这 23 个 claude 会话恢复回来？\n3. 新 daemon + 旧客户端、旧 daemon + 新客户端两种混跑组合，各会出什么问题？提交里声称\"有租约才强制\"和\"404 视作已授权\"能否真的覆盖住？\n\n## 其余评审维度\n\n4. **租约实现正确性**（cc-panes-daemon/src/server.rs、cc-panes-core/src/services/terminal_backend.rs）：TTL/续租/丢租/回收的并发正确性；续租线程持 Weak 而非 Drop 的做法是否真的解决了 Clone 语义问题；claim 与 create_session 之间的窗口期。\n5. **阶段 1 数据层**（session_restore_repo.rs）：save_sessions 改成 UPSERT 且**不再删任何行**，死行会无限增长——这个取舍的实际风险有多大？多久会出问题？移除 `tab.id` 作为 session_id 兜底是否会让某些历史数据的恢复回归？\n6. **阶段 3 认领判据**（usePanesStore.attachSessionToAnchor、useTerminalSessionRestore.adoptUnownedDaemonSessions）：判据是否足以杜绝\"把 A 项目的会话挂到 B 标签页\"？老记录缺 terminalPaneId 时按\"单 leaf 才回退\"是否安全？\n7. **新增的静默失败点**：本次改动有没有引入新的\"出错但用户看不见\"的路径？（TerminalView 的写入 catch、adopt 的 fail-closed、claim 失败只告警不回滚）\n8. **未完成项的风险**：autoAdoptDaemonSessions 默认关闭意味着正主修复不生效；接管前未查 /api/sessions/claims。这两处当前会让用户遇到什么？\n\n## 输出格式（严格三段，中文）\n\n✅ 已确认稳妥：<点列，每条 1 行>\n⚠️ 必修问题：<点列，每条标维度 + 具体文件位置 + 修改建议>\n❓ 开放问题：<点列，每条标维度 + 选项 + 你的倾向>\n\n不要复述代码内容。只列具体可执行的判断与修改点。\n\n## 收尾\n\n三段写完后，在最后单独打印一行：IMPL-REVIEW-DONE 必修=N 开放=M\n"}