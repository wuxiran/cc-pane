# 0.12.13 性能与体验优化实施记录

基线：v0.12.12 / 66d87e1020de080f2a0645cf7c2c08d8b65c3dea。
工作树：cc-book-dev-0.12.13；分支：dev/v0.12.13。
用户已批准 12 项全部纳入，视频壁纸、透明终端与 WebGL 同时工作。
侧栏交互指拖动边界调宽/收起，不新增任务拖入主区；通知声音按布局组，
免打扰仅暂停本机卡片、系统弹窗和声音，历史、未读及外部推送保留。

## 实施清单

- [x] 创建独立 dev 工作树，更新应用版本为 0.12.13。
- [x] 01/02：epoch 字符串 IPC、旧 daemon 兼容、真实 checkpoint 往返。
- [x] 01/02：透明 WebGL、8 context 预算、隐藏释放与恢复/清理。
- [x] 10/11/12：应用内存口径、排序、完整名称与可调整资源面板。
- [x] 05/06/07：可调整布局列表、保留标签的自动适配、入口左移。
- [x] 08/09：侧栏拖动/收起/取消、设置页清晰度。
- [x] 03/04：布局提示音、会话定时免打扰、后端本机通知门控。
- [x] 前端/后端完整检查与 Windows WebView2 隔离验收。

## 验收约定

正常短切组不新建 CLI、不重建 xterm、不全量恢复；真实大恢复保留静态画面。
810 万字符刷新流恢复并保存 checkpoint 后，再次恢复不超过原数据的 10%。
Windows 优化构建：15–20 会话、4 个可见终端、4K 视频、20% 终端不透明度；
50 次暖切换 P95 <=300ms，输入响应 P95 <=100ms，恢复无 >=250ms 主线程长任务。
30 分钟同负载观察与关闭资源回收；100/125/150/200% DPI 原生截图。
所有结果按实际退出码和通过/失败/未执行记录；不得用调试构建或模拟上传替代现场验收。

## 隔离边界

现有工作树与正式程序保持运行；测试使用独立配置与 daemon、合成终端。
不更改 daemon 数值协议、不做数据库迁移，不向用户会话写测试输入。
配置变更先备份；不发布正式标签或替换当前正式程序。

## 实施中验证记录

- Windows TypeScript：三轮检查退出码 0。
- 前端生产构建：退出码 0，保留既有 CSS/分块警告。
- 第一轮完整前端回归：5314/5315 通过；失败的布局遍历守卫已改用既有查询方法，正在重跑完整覆盖率。
- Windows 优化 Tauri 构建：首次退出码 0；最新合成与恢复补丁正在增量重建。
- Windows Edge 实测 NVIDIA RTX 4090：4K 视频持续播放，GPU 背景 alpha 51/255、文字 255/255；
  合成像素 [86,70,71] 对应视频 [108,87,89] 的 80%，透明度契约成立。
- 12 个 renderer 请求最多 8 个 context；隐藏释放后等待视图获配，全部销毁回到 0。
- DOM/WebGL × 历史/跟随底部四场景通过；8,100,019 字符刷新流生成 5,414 字符静态 checkpoint。
  这一浏览器测试模拟上传；真实 daemon 写入尚待桌面脚本验证，不据此宣称已完成。
- D 盘空间不足：仅本任务生成的 target 迁至 I:/ccpanes-build-cache/cc-book-dev-0.12.13，
  原 D:/04_workspace_rust/cc-book-target-dev-0.12.13 为目录联接，原项目缓存不变。
- 证据目录：D:/04_workspace_rust/cc-book-v13-evidence（不提交依赖和运行日志）。

## 最终本机结果

IMPLEMENTATION=PASS；LOCAL_ACCEPTANCE=PASS。完整记录见 ../diagnostics/v0.12.13-validation.md。
最终前端 5,323 项全部通过，现有覆盖率门槛通过；Rust 2,477 项通过、4 项原有忽略。
格式、TypeScript、workspace check、Clippy、优化构建及 git diff --check 通过。
30 分钟持续输出通过：切组点击到绘制 P95 243.8ms、输入帧 P95 12.7ms，0 context loss / 0 写入失败。
旧 daemon 精确 IPC、声音导入试听、列表/资源面板调宽通过；10 分钟脱离 CDP 后关闭 16 个标签，终端实例为 0。
私有内存在调试连接下有增长，未宣称长期无泄漏；长时与真实多屏 DPI 已建立两项扩展验证待办。
正式程序与原 daemon 保持运行，未替换正式安装包。
