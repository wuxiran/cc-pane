# Archived V1

The legacy V1 implementation is no longer stored inside the main repository tree.

Recommended sibling workspace location:

- `../archives/cc-book-v1`

Keep the archive outside the active repository so the product tree stays focused on the current Tauri/Rust/React implementation while still preserving historical reference material when needed.
