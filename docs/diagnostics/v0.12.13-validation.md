# 0.12.13 DEV 验收记录

IMPLEMENTATION=PASS
LOCAL_ACCEPTANCE=PASS
EXTENDED_VALIDATION=FOLLOW_UP

## 范围与基线

- 分支：`dev/v0.12.13`，工作树：`D:/04_workspace_rust/cc-book-dev-0.12.13`。
- 基线：正式 `v0.12.12`，`66d87e1020de080f2a0645cf7c2c08d8b65c3dea`。
- 12 项建议全部实现；第 08 项按用户确认解释为侧栏边界调宽/收起。
- 提示音按布局 ID；免打扰按会话 ID，仅暂停本机提醒。
- 原工作树未整理或回滚；正式程序与其 daemon 未替换或重启。

## 实现与验收对应

| 项目 | 实现 | 已有证据 |
|---|---|---|
| 01 切组恢复 | 精确 epoch、恢复首拍、上传合并、静态重放 | 真实 WebView2 + daemon：约 810 万字符恢复为 390 字符 checkpoint；epoch 与原生数值逐位一致 |
| 02 性能与资源回收 | 8 context 上限、可见优先、隐藏释放、复用能力探测、诊断指标 | Edge 真 GPU 12 个请求最多 8 个 context，销毁回到 0；桌面 16 会话/4 可见的 30 分钟持续输出通过；10 分钟脱离 CDP 的旧 daemon 对照通过；关闭 16 个标签后终端实例为 0 |
| 03 布局提示音 | 文件导入、试听、静音、恢复默认、布局 ID 绑定 | 交互测试与后端持久化测试通过；真实桌面 WAV 导入、试听及改名后保留同一布局 ID 的声音策略通过 |
| 04 会话免打扰 | 15 分钟/1 小时/今天/自定义、取消、到期恢复 | 真实桌面抑制弹窗并保留未读历史、取消通过；跨零点和重新加载配置由定向测试覆盖 |
| 05 布局列表 | 调宽、尺寸记忆、全名提示、选中项可见 | 布局组件与共用拖宽逻辑回归通过；真实桌面钉住列表、调宽、全名及选中项验证通过 |
| 06 自动适配 | 保留树与标签身份，仅更新方向及比例 | 真实桌面自动适配及侧栏变化后，16 个终端 DOM 实例和未提交命令保留，命令可继续执行 |
| 07 布局入口 | 视图入口左移，预设与自动适配相邻 | 顶栏组件回归通过；移除预设按钮遗留的自动左边距 |
| 08 侧栏拖拽 | 调宽、取消、拖动收起、恢复宽度 | 真实桌面检查发现并修正被裁切的热区；左右拖动和右侧收起/恢复通过 |
| 09 清晰度 | 设置内容淡入不缩放、字号调整 | 实际 Windows WebView2 在 1/1.25/1.5/2 仿真 DPR 下截图与矩阵检查通过；未修改系统显示缩放 |
| 10 应用内存口径 | 容量与整机占比，工作集统计范围说明 | 原生资源面板显示及模型字段核对通过 |
| 11 资源排序 | 分组/CPU/内存、稳定同值排序 | 单元测试及真实桌面内存排行通过 |
| 12 长名称与面板宽度 | 名称/工作空间提示、可调整宽度与记忆 | 名称与宽度逻辑测试通过；真实桌面资源面板拖宽验证通过 |

## 已完成工程检查

- Windows TypeScript：退出码 0。
- Windows 前端生产构建与 xterm 产物检查：退出码 0；保留既有 CSS 和分块体积警告。
- 最终前端全量 5,323/5,323 通过，退出码 0。覆盖率 statements 77.00%、branches 69.84%、functions 74.75%、lines 79.99%；现有配置门槛通过，未降低门槛。
- 随后 GPU/布局优化通过 51 项定向回归，侧栏命中区域通过 29 项定向回归。
- 全量回归发现的 TerminalView 测试替身接口缺失已修正；56 项组件回归及最终全量均通过。
- `cargo fmt --all -- --check`、`cargo check --workspace`、`cargo clippy --workspace -- -D warnings`：退出码均为 0。
- `cargo test --workspace --no-fail-fast`：退出码 0；共 2,477 passed，0 failed，4 项原有 ignored。
- Windows 优化 Tauri 构建：退出码 0，使用 `src-tauri/tauri.v13.dev.conf.json`，未生成或发布正式安装包。

## 浏览器与桌面证据

### 透明 WebGL

- 实际 Windows Edge / NVIDIA RTX 4090 / Direct3D11，3840×2160 视频持续播放。
- GPU 背景 alpha 为 51/255，文字 alpha 为 255/255。
- 最终页面合成像素约 `[87,71,72]`，对应视频像素 `[109,89,90]` 的 80%。
- 不仅检查 canvas alpha：同时检查外层滚动容器，避免黑底或重复背景遮住视频。
- DOM/WebGL × 历史阅读/底部跟随四场景通过；8,100,019 字符生成 5,414 字符序列化画面。该独立浏览器用例模拟上传，不能替代下述真实 IPC 验收。

### 真实桌面

- 运行优化的 Windows WebView2 程序、16 个合成 PowerShell 会话、4 个可见终端。
- 后台输出后恢复，真实 daemon 保存了 390 字符画面，delta 为 0。
- 最终 30 分钟验收：50 次切组点击到可用绘制帧 P95 243.8ms，44 个输入样本到下一完整动画帧 P95 12.7ms；每次切组均为 4 个可见 WebGL 终端。
- 自动化驱动耗时单列，不将 Playwright 等待/传输时间混为用户点击后的渲染耗时。
- 最终恢复阶段最长主线程长任务为 176ms，低于 250ms 门槛。
- 30 分钟持续输出产生 120 个采样点（首末跨度 29.92 分钟）；最大采样写入队列 20 字符、polling 为 0、context loss 为 0、写入失败为 0。旧 daemon 对照关闭 16 个测试标签后，应用内终端实例回到 0。

## 内存、兼容性与验证边界

| 场景 | renderer 私有内存：前/后 5 分钟中位数 | JS 堆：前/后 5 分钟中位数 | 结果 |
|---|---|---|---|
| 30 分钟，调试连接保持 | 550.75 → 709.48 MiB | 62.91 → 59.15 MiB | 无持续写队列或连接退化；私有内存有增长，不能据此宣称完全没有泄漏 |
| 10 分钟，脱离 CDP，复制的已安装旧 daemon | 354.68 → 358.22 MiB | 60.72 → 61.03 MiB | 对照期 renderer 相对平稳；旧协议、试听、列表操作与关闭回收通过 |

两组的持续时间和 daemon 二进制不同，不能把差异全部归因于调试器；这些窗口也不能排除更长时间的增长。
旧 daemon 对照的切组 P95 为 253.9ms，输入帧 P95 为 12.3ms，恢复最长主线程任务为 177ms。

- 同一优化主程序 SHA-256：`bef5a3df8979b751eeeed2e5277f9014aaaec9bbac08d66d8c2993db94efb783`。
- 新构建 daemon SHA-256：`4a00544fb74c37b4f591f6abbac4fc9398b37c9a21e5e3db87a3912c2a225549`。
- 复制的已安装旧 daemon SHA-256：`b8aaccb538febc924cb3d236e7801bcbbdf1444890d1b75c389a7b22534b6478`。
- 30 分钟证据：`C:/Users/wuxiran/AppData/Local/Temp/cc-v13-desktop-yhc5ef/results.json`。
- 旧 daemon/脱离连接/关闭回收证据：`C:/Users/wuxiran/AppData/Local/Temp/cc-v13-desktop-DtCbPr/results.json`。
- 两次均为独立程序、独立配置、独立浏览器目录及新启动的测试 daemon；未向现有正式会话注入测试数据。
- 四档 DPR 是实际 WebView2 内的仿真验证，未变更 Windows 宿主的系统缩放或完成真实多屏切换。

## 扩展验证回流

已建立 AI 工作项，未自动派发或改变用户系统设置：

- `6b841fd1-2bf5-454e-81f7-dad37951a672`：至少 4 小时无调试器的资源与关闭回收回归。
- `c10ed14d-5a88-4622-b335-5e2406acaf64`：真实 Windows 多屏与 100%/125%/150%/200% 系统缩放复核。

这些是扩展验证边界，不将其标为本次已经执行的测试。

## 隔离与复现

证据目录：`D:/04_workspace_rust/cc-book-v13-evidence`。
构建缓存原路径为目录联接，实际位于 `I:/ccpanes-build-cache/cc-book-dev-0.12.13`。
只迁移了本任务创建的构建缓存，原项目缓存保留。

```powershell
# 使用已构建的优化 DEV；配置、daemon、浏览器存储均独立。
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/run-v13-dev.ps1 -Optimized

# 修改代码后构建并运行。
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/run-v13-dev.ps1 -Optimized -Build
```

`CCPANES_CONFIG_DIR` 与 `CCPANES_DAEMON_DATA_DIR` 指向独立配置目录，
`WEBVIEW2_USER_DATA_FOLDER` 隔离浏览器存储，orchestrator 使用独立端口。
WebView2 的环境变量覆盖机制见 [Microsoft 官方文档](https://learn.microsoft.com/en-us/microsoft-edge/webview2/reference/win32/webview2-idl?view=webview2-1.0.4022.49)。

桌面脚本复制程序和辅助二进制后运行，使用临时项目与独立 daemon，并核对启动时间。
测试过程中不会向正式会话输入命令。清理只针对本次创建的会话和经命令行身份核对的 daemon。
调试端口仅用于测试进程，正常启动脚本默认不启用远程调试。

无数据库迁移。通知偏好写入前保存 `.bak`，随后通过临时文件替换；音频导入到应用管理目录。

## 追加压力验证（2026-09-08）

用户将四小时等待改为短时大量输出，并追加4布局/8个长对话实例切换。
[详细结果](v0.12.13-pressure-validation.md)：长对话切换60次P95 120.2ms；极限输出P95 439.7ms、最长任务648ms，未通过原时延目标。
完整草稿保留与原生多屏鼠标验收仍有验证缺口，不能据脚本退出0声明全部通过。
